import { StyleSheet } from 'react-native'

export const listStyles = StyleSheet.create({
  container: {
    flex: 1,
  },
  emptyListText: {
    flex: 1,
    textAlign: 'center',
    margin: 20,
  },
})
